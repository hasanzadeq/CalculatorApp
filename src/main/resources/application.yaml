server:
  port: 8080

spring:
  main:
    banner-mode: off

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: secret


  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: create
    show-sql: true